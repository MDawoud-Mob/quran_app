import 'package:flutter/material.dart';


class Qubla extends StatelessWidget {
  const Qubla({super.key});

  @override
  
  Widget build(BuildContext context) {
    return const Scaffold(

backgroundColor: Colors.white,

      body:Center());
  }
}

